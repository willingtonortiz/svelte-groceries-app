import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wjom.groceries',
  appName: 'groceries-app',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
